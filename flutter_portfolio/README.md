# Flutter Portfolio App

This directory contains a minimal Flutter web project that mirrors the content of the static site.

To build the web version, run:

```bash
flutter build web
```

The output will be generated under `build/web` and can be served as static files.

